# Claude Code Notes

Use `AGENTS.md` as the canonical project guide. Pipeline-specific guidance lives in `scripts/pipeline/AGENTS.md`.

This file exists only so Claude Code can discover the repo instructions without duplicating them.
